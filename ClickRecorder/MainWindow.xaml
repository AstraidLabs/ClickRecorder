<Window x:Class="ClickRecorder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:ClickRecorder.Helpers"
        xmlns:icons="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
        Title="Slaveclicker" Height="820" Width="1300"
        MinWidth="1000" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Brush.Background}">
    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="{DynamicResource Brush.SurfaceAlt}" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Slaveclicker" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource Brush.TextPrimary}"/>
                    <TextBlock Foreground="{DynamicResource Brush.TextSecondary}" FontSize="11" Margin="0,3,0,0"
                               Text="1. Krok - Napojení aplikace do nahrávání · 2. Nahrát test do přehrávání · 3. Spustit test"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Button Style="{StaticResource BGray}" Click="BtnOpenTestCases_Click" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <icons:FluentIcon Icon="AppsListDetail" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                            <TextBlock Text="Editor sekvencí" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource BGray}" Click="BtnOpenJobs_Click">
                        <StackPanel Orientation="Horizontal">
                            <icons:FluentIcon Icon="CalendarClock" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                            <TextBlock Text="Editor jobů" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- FlaUI badge -->
                <Border Grid.Column="2" Background="{DynamicResource Brush.Surface}" CornerRadius="6" Padding="10,5" Margin="0,0,10,0" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtFlaUIStatus" Text="{Binding FlaUiStatus}"
                               Foreground="{DynamicResource Brush.SectionAccent}" FontSize="11" FontWeight="SemiBold"/>
                </Border>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock Text="Téma" Foreground="{DynamicResource Brush.TextPrimary}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <CheckBox x:Name="ThemeToggle" Content="Tmavé" VerticalAlignment="Center"
                              Checked="ThemeToggle_Checked" Unchecked="ThemeToggle_Unchecked"/>
                </StackPanel>

                <Border Grid.Column="4" x:Name="StatusPill" Background="{DynamicResource Brush.SurfaceAlt}" CornerRadius="20" Padding="14,6" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtStatus" Text="{Binding StatusText}" FontSize="13" FontWeight="Bold" Foreground="{Binding StatusColor}"/>
                </Border>
            </Grid>
        </Border>

        <!-- MAIN: 4 columns -->
        <Grid Grid.Row="1" Margin="10,10,10,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>   <!-- recording + playback -->
                <ColumnDefinition Width="280"/>   <!-- recorded + inspector -->
                <ColumnDefinition Width="*"/>     <!-- exception detail -->
                <ColumnDefinition Width="300"/>   <!-- results -->
            </Grid.ColumnDefinitions>

            <!-- ── COL 0: Recording + Playback panels ─────────────────────── -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Expander Grid.Row="1" IsExpanded="True" Style="{StaticResource PanelExpander}" Margin="0,0,8,8">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="NAHRÁVÁNÍ" VerticalAlignment="Center"/>
                            <Border Background="{DynamicResource Brush.SurfaceAlt}" BorderBrush="{DynamicResource Brush.SectionAccent}" BorderThickness="1" CornerRadius="10" Width="18" Height="18" Margin="6,0,0,0" VerticalAlignment="Center" ToolTipService.ShowDuration="60000" Cursor="Hand">
                                <Border.ToolTip>
                                    <ToolTip Placement="Bottom" Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" Foreground="{DynamicResource Brush.TextPrimary}" Padding="10" MaxWidth="360">
                                        <TextBlock TextWrapping="Wrap" FontSize="11" Text="Návod: Vyberte režim zachytávání, klikněte na Nahrát, proveďte kroky a ukončete tlačítkem Stop. Po zastavení se zobrazí nabídka pro uložení nebo zahození záznamu."/>
                                    </ToolTip>
                                </Border.ToolTip>
                                <icons:FluentIcon Icon="Info" Width="12" Height="12" Foreground="{DynamicResource Brush.SectionAccent}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Expander.Header>
                    <Border Style="{StaticResource PanelContentCard}" Margin="0,4,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,-2,0,0">
                            <TextBlock Text="Spusťte nahrávání kliknutím na tlačítko Nahrát." Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                            <StackPanel Margin="0,0,0,6">
                                <Button x:Name="BtnRecord" Style="{StaticResource BPurple}"
                                        Click="BtnRecord_Click" Margin="0,0,0,6"
                                        HorizontalAlignment="Stretch" IsEnabled="{Binding CanRecord}">
                                    <StackPanel Orientation="Horizontal">
                                        <icons:FluentIcon Icon="Record" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        <TextBlock Text="Nahrát" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="BtnStopRecord" Style="{StaticResource BRed}"
                                        Click="BtnStopRecord_Click" HorizontalAlignment="Stretch">
                                    <StackPanel Orientation="Horizontal">
                                        <icons:FluentIcon Icon="Stop" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        <TextBlock Text="Stop" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                            <TextBlock Text="Režim zachytávání" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                            <RadioButton Content="Podle UI elementu (FlaUI)" GroupName="CaptureMode"
                                         IsChecked="{Binding CaptureByElement}" Margin="0,0,0,2"/>
                            <RadioButton Content="Podle souřadnic" GroupName="CaptureMode"
                                         IsChecked="{Binding CaptureByCoordinates}" Margin="0,0,0,6"/>
                            <Button x:Name="BtnClear" Content="Smazat záznam" Style="{StaticResource BOrange}"
                                    Click="BtnClear_Click" HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Expander>

                <Expander Grid.Row="0" IsExpanded="True" Style="{StaticResource PanelExpander}" Margin="0,0,8,8">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="NAPOJENÍ APLIKACE" VerticalAlignment="Center"/>
                            <Border Background="{DynamicResource Brush.SurfaceAlt}" BorderBrush="{DynamicResource Brush.SectionAccent}" BorderThickness="1" CornerRadius="10" Width="18" Height="18" Margin="6,0,0,0" VerticalAlignment="Center" ToolTipService.ShowDuration="60000" Cursor="Hand">
                                <Border.ToolTip>
                                    <ToolTip Placement="Bottom" Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" Foreground="{DynamicResource Brush.TextPrimary}" Padding="10" MaxWidth="360">
                                        <TextBlock TextWrapping="Wrap" FontSize="11" Text="Pro přesné nahrávání nejprve připojte cílovou aplikaci – nahrávání se omezí jen na ni a snížíte riziko chybných kliknutí."/>
                                    </ToolTip>
                                </Border.ToolTip>
                                <icons:FluentIcon Icon="Info" Width="12" Height="12" Foreground="{DynamicResource Brush.SectionAccent}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Expander.Header>
                    <Border Style="{StaticResource PanelContentCard}" Margin="0,4,0,0">
                        <StackPanel Margin="0,-2,0,0">
                            <TextBlock Text="Připojit aplikaci kliknutím do oblasti aplikace." Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                            <Button x:Name="BtnAttachApp" Style="{StaticResource BPurple}"
                                    Click="BtnAttachApp_Click" HorizontalAlignment="Stretch" Margin="0,0,0,6"
                                    IsEnabled="{Binding CanAttachApplication}">
                                <StackPanel Orientation="Horizontal">
                                    <icons:FluentIcon Icon="PlugConnected" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    <TextBlock Text="{Binding AttachButtonText}" Margin="6,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="BtnDetachApp" Style="{StaticResource BRed}"
                                    Click="BtnDetachApp_Click" HorizontalAlignment="Stretch" Margin="0,0,0,6"
                                    IsEnabled="{Binding CanDetachApplication}">
                                <StackPanel Orientation="Horizontal">
                                    <icons:FluentIcon Icon="PlugDisconnected" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    <TextBlock Text="Odpojit aplikaci" Margin="6,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <TextBlock Text="{Binding AttachedAppText}" Foreground="{DynamicResource Brush.TextPrimary}" FontSize="11" Margin="0,0,0,4" TextWrapping="Wrap"/>

                        </StackPanel>
                    </Border>
                </Expander>

                <Expander Grid.Row="2" IsExpanded="True" Style="{StaticResource PanelExpander}" Margin="0,0,8,0">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="PŘEHRÁVÁNÍ" VerticalAlignment="Center"/>
                            <Border Background="{DynamicResource Brush.SurfaceAlt}" BorderBrush="{DynamicResource Brush.SectionAccent}" BorderThickness="1" CornerRadius="10" Width="18" Height="18" Margin="6,0,0,0" VerticalAlignment="Center" ToolTipService.ShowDuration="60000" Cursor="Hand">
                                <Border.ToolTip>
                                    <ToolTip Placement="Bottom" Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" Foreground="{DynamicResource Brush.TextPrimary}" Padding="10" MaxWidth="360">
                                        <TextBlock TextWrapping="Wrap" FontSize="11" Text="Návod: Nastavte opakování a rychlost, zvolte chování při chybě a spusťte test tlačítkem Přehrát. Pokud potřebujete, běh zastavíte tlačítkem Stop."/>
                                    </ToolTip>
                                </Border.ToolTip>
                                <icons:FluentIcon Icon="Info" Width="12" Height="12" Foreground="{DynamicResource Brush.SectionAccent}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Expander.Header>
                    <Border Style="{StaticResource PanelContentCard}" Margin="0,4,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,-2,0,0">
                            <TextBlock Text="Spusťte přehrávání tlačítkem Přehrát nebo načtěte uložený test." Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                    <TextBlock Text="Opakování" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                                    <TextBox x:Name="TxtRepeat" Text="{Binding RepeatText, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Rychlost ×" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                                    <TextBox x:Name="TxtSpeed" Text="{Binding SpeedText, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </Grid>
                            <CheckBox x:Name="ChkStopOnError"  Content="Zastavit při první chybě" Margin="0,2,0,4" IsChecked="{Binding StopOnError}"/>
                            <CheckBox x:Name="ChkScreenshots"  Content="Screenshot při chybě"     Margin="0,0,0,10" IsChecked="{Binding TakeScreenshots}"/>
                            <StackPanel Margin="0,0,0,0">
                                <Button x:Name="BtnLoadPlayback" Style="{StaticResource BBlue}"
                                        Click="BtnLoadPlayback_Click" Margin="0,0,0,6" HorizontalAlignment="Stretch">
                                    <StackPanel Orientation="Horizontal">
                                        <icons:FluentIcon Icon="Folder" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        <TextBlock Text="Načíst" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="BtnPlay" Style="{StaticResource BGreen}"
                                        Click="BtnPlay_Click" Margin="0,0,0,6" HorizontalAlignment="Stretch" IsEnabled="{Binding CanPlay}">
                                    <StackPanel Orientation="Horizontal">
                                        <icons:FluentIcon Icon="Play" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        <TextBlock Text="Přehrát" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="BtnStopPlay" Style="{StaticResource BRed}"
                                        Click="BtnStopPlay_Click" HorizontalAlignment="Stretch" IsEnabled="{Binding CanStopPlayback}">
                                    <StackPanel Orientation="Horizontal">
                                        <icons:FluentIcon Icon="Stop" Width="14" Height="14" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        <TextBlock Text="Stop" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                        <Separator/>

                        <TextBlock Text="EXPORT" Style="{StaticResource Sec}"/>
                        <Button x:Name="BtnExport" Content="📄  HTML Report" Style="{StaticResource BBlue}"
                                Click="BtnExport_Click" HorizontalAlignment="Stretch"/>

                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Expander>
            </Grid>

            <!-- ── COL 1: Recorded list + selected element inspector ─────── -->
            <Grid Grid.Column="1" Margin="0,0,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="160"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <Border Style="{StaticResource Card}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="NAHRANÁ KLIKNUTÍ" Style="{StaticResource Sec}" DockPanel.Dock="Left" Margin="0"/>
                            <TextBlock x:Name="TxtRecordCount" Text="{Binding RecordCount}" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="11"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </DockPanel>
                        <ListBox x:Name="ClickList" Grid.Row="1" ItemsSource="{Binding Clicks}"
                                 SelectionChanged="ClickList_SelectionChanged"/>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,10,0,6">
                    <StackPanel>
                        <TextBlock Text="VYBRANÝ KROK" Style="{StaticResource Sec}" Margin="0,0,0,4"/>
                        <TextBlock Text="Název kroku" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                        <TextBox x:Name="TxtStepName" Text="{Binding SelectedStepName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6"/>
                        <TextBlock Text="Popis kroku" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="10" Margin="0,0,0,3"/>
                        <TextBox x:Name="TxtStepDescription" Text="{Binding SelectedStepDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnStepUp" Content="Nahoru" Style="{StaticResource BGray}" Click="BtnStepUp_Click" Margin="0,0,6,0"/>
                            <Button x:Name="BtnStepDown" Content="Dolů" Style="{StaticResource BGray}" Click="BtnStepDown_Click" Margin="0,0,6,0"/>
                            <Button x:Name="BtnApplyStep" Content="Uložit krok" Style="{StaticResource BBlue}" Click="BtnApplyStep_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="3" Text="ELEMENT INSPEKTOR" Style="{StaticResource Sec}" Margin="0,10,0,6"/>

                <Border Grid.Row="4" Style="{StaticResource Card}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="InspectorPanel">
                            <TextBlock x:Name="TxtInspHint" Text="← Vyber záznam"
                                       Foreground="{DynamicResource Brush.TextSecondary}" FontSize="11" TextWrapping="Wrap"/>

                            <!-- Rows filled from code -->
                            <Grid x:Name="InspectorGrid" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/><RowDefinition/><RowDefinition/>
                                    <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                                </Grid.RowDefinitions>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- ── COL 2: Global exceptions ──────────────────────────────── -->
            <Border Grid.Column="2" Style="{StaticResource Card}" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="GLOBÁLNÍ VÝJIMKY" Style="{StaticResource Sec}"/>
                    <ListBox x:Name="GlobalExList" Grid.Row="1" ItemsSource="{Binding GlobalExceptions}"
                             SelectionChanged="GlobalExList_SelectionChanged"/>
                </Grid>
            </Border>

            <!-- ── COL 3: Test results ────────────────────────────────────── -->
            <Border Grid.Column="3" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,0,0,10">
                        <TextBlock Text="VÝSLEDKY TESTU" Style="{StaticResource Sec}" DockPanel.Dock="Left" Margin="0"/>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                            <Button x:Name="BtnTicket" Style="{StaticResource BPurple}"
                                    Click="BtnTicket_Click" FontSize="10" Padding="8,4"
                                    IsEnabled="False" Margin="0,0,6,0">
                                <StackPanel Orientation="Horizontal">
                                    <icons:FluentIcon Icon="ClipboardText" Width="12" Height="12" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    <TextBlock Text="Ticket" Margin="4,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button Content="Vymazat" Style="{StaticResource BGray}"
                                    Click="BtnClearResults_Click" FontSize="10" Padding="8,4"/>
                        </StackPanel>
                    </DockPanel>
                    <ListBox x:Name="ResultList" Grid.Row="1" ItemsSource="{Binding Results}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Button Grid.Column="1" Content="Detail" Style="{StaticResource BBlue}" FontSize="10" Padding="8,3"
                                            Tag="{Binding}" Click="BtnResultDetail_Click"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- STATUS BAR -->
        <Border Grid.Row="2" Background="{DynamicResource Brush.SurfaceAlt}" Padding="14,6" Margin="10,0,10,8" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TxtFooter" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="11" Text="{Binding FooterText}" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="STATISTIKY:" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="11" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TxtOk" Text="{Binding OkText}" Foreground="{DynamicResource Brush.ButtonSuccessBg}" FontWeight="Bold" FontSize="11" Margin="0,0,12,0"/>
                    <TextBlock x:Name="TxtErr" Text="{Binding ErrText}" Foreground="{DynamicResource Brush.ButtonDangerBg}" FontWeight="Bold" FontSize="11" Margin="0,0,12,0"/>
                    <TextBlock x:Name="TxtFlaUI" Text="{Binding FlaUiText}" Foreground="{DynamicResource Brush.SectionAccent}" FontSize="11" Margin="0,0,12,0"/>
                    <TextBlock x:Name="TxtCoord" Text="{Binding CoordText}" Foreground="{DynamicResource Brush.ButtonWarningBg}" FontSize="11" Margin="0,0,12,0"/>
                    <TextBlock x:Name="TxtDur" Text="{Binding DurationText}" Foreground="{DynamicResource Brush.TextSecondary}" FontSize="11"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
